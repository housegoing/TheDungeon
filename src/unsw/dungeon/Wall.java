package unsw.dungeon;

public class Wall extends Entity {
    /**
     * Constructor initialize wall instance on position (x,y)
     * @param x
     * @param y
     */
    public Wall(int x, int y) {
        super(x, y);
    }

}
