package unsw.dungeon;

public class Apple extends Pickable{
    /**
     * Constructor for initializing the apple instance on position (x,y)
     * @param x
     * @param y
     */
    public Apple(int x, int y){
        super(x, y);
    }
}