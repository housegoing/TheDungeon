package unsw.dungeon;

public class Treasure extends Pickable{
    /**
     * Constructor initialize treasure instance on position (x,y)
     * @param x
     * @param y
     */
    public Treasure(int x, int y) {
        super(x, y);
    }
}